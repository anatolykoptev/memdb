// Chat service definitions for MemDB internal RPC.
// Includes streaming support for real-time chat responses.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: memdb/v1/chat.proto

package memdbv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/anatolykoptev/memdb/memdb-go/gen/proto/memdb/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ChatServiceName is the fully-qualified name of the ChatService service.
	ChatServiceName = "memdb.v1.ChatService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ChatServiceCompleteProcedure is the fully-qualified name of the ChatService's Complete RPC.
	ChatServiceCompleteProcedure = "/memdb.v1.ChatService/Complete"
	// ChatServiceStreamProcedure is the fully-qualified name of the ChatService's Stream RPC.
	ChatServiceStreamProcedure = "/memdb.v1.ChatService/Stream"
)

// ChatServiceClient is a client for the memdb.v1.ChatService service.
type ChatServiceClient interface {
	// Complete chat response (non-streaming).
	Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error)
	// Stream chat response via server-side streaming.
	Stream(context.Context, *connect.Request[v1.StreamRequest]) (*connect.ServerStreamForClient[v1.StreamResponse], error)
}

// NewChatServiceClient constructs a client for the memdb.v1.ChatService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewChatServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ChatServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	chatServiceMethods := v1.File_memdb_v1_chat_proto.Services().ByName("ChatService").Methods()
	return &chatServiceClient{
		complete: connect.NewClient[v1.CompleteRequest, v1.CompleteResponse](
			httpClient,
			baseURL+ChatServiceCompleteProcedure,
			connect.WithSchema(chatServiceMethods.ByName("Complete")),
			connect.WithClientOptions(opts...),
		),
		stream: connect.NewClient[v1.StreamRequest, v1.StreamResponse](
			httpClient,
			baseURL+ChatServiceStreamProcedure,
			connect.WithSchema(chatServiceMethods.ByName("Stream")),
			connect.WithClientOptions(opts...),
		),
	}
}

// chatServiceClient implements ChatServiceClient.
type chatServiceClient struct {
	complete *connect.Client[v1.CompleteRequest, v1.CompleteResponse]
	stream   *connect.Client[v1.StreamRequest, v1.StreamResponse]
}

// Complete calls memdb.v1.ChatService.Complete.
func (c *chatServiceClient) Complete(ctx context.Context, req *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error) {
	return c.complete.CallUnary(ctx, req)
}

// Stream calls memdb.v1.ChatService.Stream.
func (c *chatServiceClient) Stream(ctx context.Context, req *connect.Request[v1.StreamRequest]) (*connect.ServerStreamForClient[v1.StreamResponse], error) {
	return c.stream.CallServerStream(ctx, req)
}

// ChatServiceHandler is an implementation of the memdb.v1.ChatService service.
type ChatServiceHandler interface {
	// Complete chat response (non-streaming).
	Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error)
	// Stream chat response via server-side streaming.
	Stream(context.Context, *connect.Request[v1.StreamRequest], *connect.ServerStream[v1.StreamResponse]) error
}

// NewChatServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewChatServiceHandler(svc ChatServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	chatServiceMethods := v1.File_memdb_v1_chat_proto.Services().ByName("ChatService").Methods()
	chatServiceCompleteHandler := connect.NewUnaryHandler(
		ChatServiceCompleteProcedure,
		svc.Complete,
		connect.WithSchema(chatServiceMethods.ByName("Complete")),
		connect.WithHandlerOptions(opts...),
	)
	chatServiceStreamHandler := connect.NewServerStreamHandler(
		ChatServiceStreamProcedure,
		svc.Stream,
		connect.WithSchema(chatServiceMethods.ByName("Stream")),
		connect.WithHandlerOptions(opts...),
	)
	return "/memdb.v1.ChatService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ChatServiceCompleteProcedure:
			chatServiceCompleteHandler.ServeHTTP(w, r)
		case ChatServiceStreamProcedure:
			chatServiceStreamHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedChatServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedChatServiceHandler struct{}

func (UnimplementedChatServiceHandler) Complete(context.Context, *connect.Request[v1.CompleteRequest]) (*connect.Response[v1.CompleteResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("memdb.v1.ChatService.Complete is not implemented"))
}

func (UnimplementedChatServiceHandler) Stream(context.Context, *connect.Request[v1.StreamRequest], *connect.ServerStream[v1.StreamResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("memdb.v1.ChatService.Stream is not implemented"))
}
